<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Hard-restricted: require installer allowlist (ADB / managed) before grant. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!--
      Role/signature: private ADB appops for timely OTP/sensitive SMS (not Settings-grantable).
      Lint ProtectedPermissions is intentional for this private sideload product.
    -->
    <uses-permission
        android:name="android.permission.RECEIVE_SENSITIVE_NOTIFICATIONS"
        tools:ignore="ProtectedPermissions" />

    <!-- SMS permissions imply telephony; keep optional for non-phone form factors. -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- No INTERNET, READ_SMS, contacts, MMS, location, or storage. -->

    <application
        android:name=".SmsForwarderApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsForwarder">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exported; protected by BROADCAST_SMS (only system SMS dispatcher). -->
        <receiver
            android:name=".telephony.SmsReceivedReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Not direct-boot-aware; credential-protected storage only. -->
        <receiver
            android:name=".telephony.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".telephony.SendResultReceiver"
            android:exported="false" />

        <receiver
            android:name=".telephony.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.johnc4rl0.smsforwarder.action.PAUSE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
